import React, {FC} from "react";
import {Order} from "@/types";

interface Props {
    order: Order
}

const OrderModal: FC<Props> = (
    {
      order
    }
) => {
    return (
        <div>

        </div>
    )
}

export default OrderModal
